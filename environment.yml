name: quantum-solow-env

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - numpy>=1.24
  - pandas>=2.0
  - scipy>=1.11
  - scikit-learn>=1.3
  - matplotlib>=3.7
  - openpyxl>=3.1
  - statsmodels>=0.14

  # Optional: nếu bạn dùng thêm (từ code: IterativeImputer từ sklearn, nhưng đã có scikit-learn)
  # - pip  # nếu cần pip install gì thêm

  # pip section nếu có package chỉ có trên PyPI (hiện tại không cần)
  # - pip:
  #   - some-pypi-only-package==version